import { ShoppingListManager } from '@/components/shopping-lists/ShoppingListManager';

export default function ShoppingListsPage() {
  return (
    <div className="container mx-auto py-8">
      <ShoppingListManager />
    </div>
  );
}
